<%= semantic_form_for @message do |f| %>
  <%= f.input :name %>
  <%= f.input :email %>
  <%= f.input :content, :label => ft(:message) %>
<%= f.submit t2(:send,:message), :class => :submit %>
<% end %>
